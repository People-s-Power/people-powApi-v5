type Post {
  _id: ID
  body: String!
  petition: Petition!
  author: User!
  likes: Int!
  comments: [Comment!]!
  shares: Int!
  isPetition: Boolean
  image: String!
  createdAt: Date
  updatedAt: Date
}

type Comment {
  author: String!
  body: String! 
}

type Query {
  getPosts(limit: Int): [Post!]!
  getPost(id: ID!): Post!
  myPosts: [Post!]!
}

type Mutation {
  createPost(body: String, imageFile: String): Post!
  updatePost(body: String, postId: ID): Post!
  updateImg(imageFile: String, postId: ID): Post!
}
